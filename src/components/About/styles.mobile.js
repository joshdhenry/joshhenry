const about = {}

const buttons = {
  display: 'flex',
  flex: 0.85,
  flexDirection: 'row',
  marginLeft: '1rem',
  marginRight: '1rem',
  width: '50%',
}

const buttonsWrapper = {
  display: 'flex',
  flex: 1,
  flexDirection: 'row',
  justifyContent: 'center',
}

const image = {
  width: '50%',
}

export const paragraph = {
  margin: '0.5rem 1rem',
  textAlign: 'left',
}

const wrapper = {
  alignItems: 'center',
  display: 'flex',
  flex: 1,
  flexDirection: 'column',
  justifyContent: 'center',
  margin: '1rem',
}

export default {
  buttons,
  buttonsWrapper,
  about,
  image,
  paragraph,
  wrapper,
}

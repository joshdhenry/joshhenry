const projectImage = {
  backgroundColor: 'blue',
  display: 'flex',
  flex: 1,
  justifyContent: 'flex-end',
}

const technology = {
  height: 32,
}

const text = { backgroundColor: 'red', flex: 1 }

const wrapper = {
  display: 'flex',
  flex: 1,
  flexDirection: 'row',
}

export default {
  projectImage,
  technology,
  text,
  wrapper,
}

const image = { width: '15rem' }

const imageWrapper = {
  display: 'flex',
  flex: 1,
  justifyContent: 'center',
  padding: '0rem',
}

const technology = {
  height: '2rem',
  padding: '0.5rem',
}

const text = {
  flex: 1,
  padding: '2rem',
  textAlign: 'center',
}

const wrapper = {
  display: 'flex',
  flex: 1,
  flexDirection: 'column',
}

export default {
  image,
  imageWrapper,
  technology,
  text,
  wrapper,
}
